# examples/settings

Runnable settings demo that uses the recommended-actions package and optional live embeddings.

## Directories

- None.

## Files

- `.env.example`: Example environment variables for enabling live OpenAI recommendations.
- `App.tsx`: Main React app and recommended-actions flow for the settings example.
- `index.css`: Example-specific styles for the settings demo.
- `index.html`: Vite HTML entry point for the settings example.
- `main.tsx`: React bootstrap for the settings example.
- `package.json`: Workspace package manifest and scripts for the settings example.
- `tsconfig.json`: TypeScript configuration for the settings example.
- `vite-env.d.ts`: Vite environment type declarations for the settings example.
- `vite.config.ts`: Vite configuration for the settings example.

## Writing Rules

- No additional local writing rules.
- Follow inherited AGENTS.md guidance when applicable.
