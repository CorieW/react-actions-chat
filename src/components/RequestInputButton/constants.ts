/**
 * Shared id used by the persistent abort button for input-request flows.
 */
export const ABORT_BUTTON_ID = 'input-request-abort';

/**
 * Placeholder shown while an async reply is still in progress.
 */
export const WAITING_FOR_TURN_PLACEHOLDER = 'Waiting for a response...';
