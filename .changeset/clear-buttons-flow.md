---
'react-actions-chat': minor
---

Add select input support to guided request flows. The main package now exports `InputSelectOption`, stores select options on the shared input field, renders `select` mode in `InputBar`, and lets request input buttons pass options with `inputOptions`.
