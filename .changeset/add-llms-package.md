---
'react-actions-chat-llms': minor
---

Add a new `react-actions-chat-llms` companion package with backend-routed
text-generation helpers, a runnable `llm-support` example, and shared client
utilities for calling a local text-generation backend from chat flows.
